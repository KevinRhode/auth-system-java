{
  "buildCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then npm run build -- --configuration production; else npm run build -- --configuration dev; fi",
  "outputDirectory": "dist/frontend/browser",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}